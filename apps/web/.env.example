# Web App Environment Variables
# Copy this file to .env.local and fill in your values

# API Service URL
# Local development: http://localhost:4000
# Production (Railway): https://your-api-service.up.railway.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# MongoDB Connection (optional - for web app's /api routes if still in use)
# Use MongoDB Atlas connection string
# Note: @ symbol in password must be URL-encoded as %40
# Example: mongodb+srv://username:Pass%40word@cluster.mongodb.net/database
MONGODB_URI=mongodb://localhost:27017/code-runner

# Optional: Database name (if using custom database)
MONGODB_DB=code-runner

# Environment
NODE_ENV=development
