# Runner Service Environment Variables
# Copy this file to .env.local and fill in your values

# Environment
NODE_ENV=development

# MongoDB Atlas Database
# IMPORTANT: Must match the same database as API service
# IMPORTANT: @ symbol in password must be URL-encoded as %40
# Railway: Set this in Railway dashboard environment variables
MONGODB_URI=mongodb://localhost:27017/code-runner

# Redis Configuration
# Must connect to the same Redis instance as API service
# Railway: Railway will inject REDIS_URL automatically via Redis plugin
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Worker Configuration
# Number of concurrent jobs each worker can process
# Adjust based on available resources
WORKER_CONCURRENCY_EXECUTION=5
WORKER_CONCURRENCY_SUBMISSION=3

# Docker Configuration (usually auto-detected)
# Only needed if Railway supports Docker-in-Docker
# DOCKER_SOCKET=/var/run/docker.sock
